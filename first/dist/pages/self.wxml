
  <view>
    <view class="login">
      <view class="login1" bindtap="onBack">{{_login}}</view>
    </view>
    <view class="background">
      <image class="circle" src="{{pic}}"/>
      <view class="name">{{name}}</view>
    </view>
    <view class="want">想用</view>
    <view class="collection">
      <image class="big" src="{{icon}}"/>
      <view style="font-size: 0.8rem">{{title}}</view>
    </view>
    <view class="line"/>
    <view class="want">用过</view>
    <view class="collection">
      <image class="big" src="{{_icon}}"/>
      <view style="font-size: 0.8rem">{{_title}}</view>
    </view>
  </view>